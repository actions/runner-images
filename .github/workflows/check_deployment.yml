name: Build Image
on: [push, pull_request]

jobs:
  build:
    name: "BuildImage"
    runs-on: ubuntu-latest
    steps:

      - name: Hello world
        run: echo check work machine

      - name: Environment variables
        run: printenv

      - name: Exit
        run: exit 1